--- dbus-1.0.0/doc/Makefile.am	2005-08-18 07:04:57.000000000 +0300
+++ dbus-1.0.0.new/doc/Makefile.am	2006-11-13 15:01:57.560235448 +0200
@@ -36,3 +36,12 @@
 
 maintainer-clean-local:
 	rm -f $(HTML_FILES)
+
+install-data-local:
+	if test -d api; then \
+          mkdir -p $(DESTDIR)$(datadir)/doc/dbus/html; \
+          for F in `find api/html -type f`; do \
+            B=`basename $$F`; \
+            $(INSTALL_DATA) $$F $(DESTDIR)$(datadir)/doc/dbus/html/$$B; \
+          done \
+        fi
