diff -pur dbus-1.0.2/dbus/dbus-spawn.c dbus-1.0.2.new/dbus/dbus-spawn.c
--- dbus-1.0.2/dbus/dbus-spawn.c	2006-12-11 21:21:09.000000000 +0200
+++ dbus-1.0.2.new/dbus/dbus-spawn.c	2007-08-13 11:09:52.564851752 +0300
@@ -847,6 +847,13 @@ do_exec (int                       child
 #ifdef DBUS_BUILD_TESTS
   int i, max_open;
 #endif
+  /* Change oom protection to off to prevent inheritance from dbus server */
+  int oom = open ("/proc/self/oom_adj", O_WRONLY|O_SYNC);
+  if (oom >= 0)
+   {
+     write (oom, "0", sizeof (char));
+     _dbus_close (oom, NULL);
+   }
 
   _dbus_verbose_reset ();
   _dbus_verbose ("Child process has PID %lu\n",
