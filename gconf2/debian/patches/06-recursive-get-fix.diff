diff -ur gconf-dbus-2.6.4.13/gconf/gconf-dbus-utils.c gconf-dbus-2.6.4.13.new/gconf/gconf-dbus-utils.c
--- gconf-dbus-2.6.4.13/gconf/gconf-dbus-utils.c	2005-11-22 15:43:09.000000000 +0200
+++ gconf-dbus-2.6.4.13.new/gconf/gconf-dbus-utils.c	2006-01-19 19:31:43.302295336 +0200
@@ -1084,7 +1084,7 @@
   dbus_message_iter_recurse (iter, &array_iter);
 
   /* Loop through while there are structs (entries). */
-  while (dbus_message_iter_get_arg_type (iter) == DBUS_TYPE_STRUCT)
+  while (dbus_message_iter_get_arg_type (&array_iter) == DBUS_TYPE_STRUCT)
     {
       gchar      *key;
       GConfValue *value;
@@ -1093,14 +1093,14 @@
       gchar      *schema_name;
       GConfEntry *entry;
 
-      if (!utils_get_entry_values_stringified (iter,
+      if (!utils_get_entry_values_stringified (&array_iter,
 					       &key,
 					       &value,
 					       &is_default,
 					       &is_writable,
 					       &schema_name))
 	break;
-      
+
       entry = gconf_entry_new_nocopy (gconf_concat_dir_and_key (dir, key), value);
 
       gconf_entry_set_is_default (entry, is_default);
@@ -1111,7 +1111,7 @@
       
       entries = g_slist_prepend (entries, entry);
       
-      dbus_message_iter_next (iter);
+      dbus_message_iter_next (&array_iter);
     }
 
   return entries;
