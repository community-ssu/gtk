#! /bin/sh -e

# replace the doc directory with a doc symlink
pkg=`basename $0 .postinst`
if [ ! -L  /usr/share/doc/$pkg ]; then
    rm -rf /usr/share/doc/$pkg
    ln -s python-numeric /usr/share/doc/$pkg
else
    true
fi

#DEBHELPER#
