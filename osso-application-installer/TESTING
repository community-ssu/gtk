Test repository / files
-----------------------

- should have package that is listed in Packages but the .deb is not there.

- should have corrupted package / wrong checksum

- should have broken package / failing postinst script

- should have old maemo packages

- package whose postinst fails

- package that pops up dialog

- package that pops up Location selection dialog

- package that conflicts with other package

- package that depends on other package

- package with missing dependencies

- there should be a big package

- there should be a non-user package

- there should be Nokia-certified and non-Nokia-certfied packages

- there should be different generations of repositories to test
  upgrading

- repo should be on web
