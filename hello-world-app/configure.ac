AC_INIT(Makefile.am)
AM_INIT_AUTOMAKE(hello-world-app, 0.4)

AC_PROG_CPP
AC_PROG_INSTALL
AC_PROG_LIBTOOL

PKG_CHECK_MODULES(GTK, gtk+-2.0)
AC_SUBST(GTK_LIBS)
AC_SUBST(GTK_CFLAGS)

PKG_CHECK_MODULES(OSSO, libosso >= 1)
AC_SUBST(OSSO_LIBS)
AC_SUBST(OSSO_CFLAGS)

AC_OUTPUT([ Makefile ])
